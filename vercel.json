{
    "version": 2,
    "builds": [
      {
        "src": "pwa-moveis/package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "pwa-moveis/public" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "pwa-moveis/$1" }
    ]
  }
  